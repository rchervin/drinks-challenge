%h1 Search

%form#twitter-form
  %input{name: 'screen_name', type: 'text'}
  %button#tweetbutton{type: 'submit', action: 'search', method: :post, remote: true}
    Search Tweets

.tweetbox
  - if !@tweets.nil?
    - @tweets.each do |tweet|
      %p= tweet.text